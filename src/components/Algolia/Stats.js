import React from "react";

const Stats = (results) => {

    return (
        <div className="search-results-heading">{results.nbHits} Properties for Sale in West Yorkshire</div>
    );
}

export default Stats